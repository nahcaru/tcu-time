/**
 * Desktop sidebar navigation.
 *
 * Uses shadcn/ui Sidebar component (to be added).
 * - Collapsed: 80px (icon only)
 * - Expanded: 256px
 * - Hover to expand (legacy compat)
 *
 * Sections:
 * - Header: Logo ("TiME" collapsed / "TCU-TIME" expanded)
 * - Menu: 科目一覧 (IconList), 時間割 (IconTableFilled)
 * - Footer: Theme toggle, User avatar/login, Info
 */
export function AppSidebar() {
  return null // Phase 2 implementation
}
