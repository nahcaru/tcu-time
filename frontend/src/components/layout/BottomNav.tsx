/**
 * Mobile bottom navigation bar.
 *
 * Visible only on mobile (< 1024px).
 * Two items:
 * - 科目一覧 (IconList) → /
 * - 時間割 (IconTableFilled) → /timetable
 */
export function BottomNav() {
  return null // Phase 2 implementation
}
