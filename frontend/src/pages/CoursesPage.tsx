/**
 * 科目一覧画面 (/)
 *
 * Default screen. Course search, filter, and list.
 * Components: FilterPanel, SearchBar, CourseList, CourseCard, CourseDialog
 */
export function CoursesPage() {
  return (
    <div className="p-4">
      <h1 className="text-lg font-medium">科目一覧</h1>
      <p className="text-sm text-muted-foreground">Phase 2 で実装</p>
    </div>
  )
}
