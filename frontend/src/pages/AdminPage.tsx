/**
 * 管理画面 (/admin)
 *
 * Extraction status list, review/approve panel, re-extraction trigger.
 * Requires admin role.
 */
export function AdminPage() {
  return (
    <div className="p-4">
      <h1 className="text-lg font-medium">管理画面</h1>
      <p className="text-sm text-muted-foreground">Phase 4 で実装</p>
    </div>
  )
}
