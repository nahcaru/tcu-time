/**
 * 時間割画面 (/timetable)
 *
 * Weekly grid (Mon-Sat x 5 periods) + credits tracker.
 * Components: CourseGrid, GridCell, TimeSlots, IntensiveCourses, CreditsTable
 */
export function TimetablePage() {
  return (
    <div className="p-4">
      <h1 className="text-lg font-medium">時間割</h1>
      <p className="text-sm text-muted-foreground">Phase 3 で実装</p>
    </div>
  )
}
